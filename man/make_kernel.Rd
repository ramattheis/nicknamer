% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/make_kernel.R
\name{make_kernel}
\alias{make_kernel}
\title{Build the transition kernel matrix for the garbling model}
\usage{
make_kernel(D, M, delta, lambda)
}
\arguments{
\item{D}{Matrix or sparseMatrix; K×K distance matrix between names.}

\item{M}{Matrix or sparseMatrix; K×K binary mask for allowed mutations (1 = neighbor, 0 otherwise).}

\item{delta}{Numeric in [0,1]; probability mass moved off the diagonal.}

\item{lambda}{Numeric > 0; exponential decay rate for off-diagonal mutations.}
}
\value{
K×K matrix; entry (i,j) = P(observed = i | true = j).
}
\description{
Constructs the K×K kernel of P(obs = i | true = j) by
1. Computing an unnormalized weight matrix W = M \%*\% exp(-lambda * D),
2. Row-normalizing W to obtain mutation probabilities E, and
3. Combining with the garbling probability \code{delta} to form Kmat = delta * t(E) + diag(1 - delta).
}
\examples{
# assume D_mat and M_mat are defined
Kmat <- make_kernel(D_mat, M_mat, delta = 0.1, lambda = 1)

}
