% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/draw_gibbs.R
\name{draw_gibbs}
\alias{draw_gibbs}
\title{Draw from the posterior of the name garbling model.}
\usage{
draw_gibbs(
  p_init,
  D,
  M,
  n_obs,
  n_iter = 10000,
  delta_init = 0.1,
  lambda_init = 1,
  sd_logit = 0.1,
  sd_loglam = 0.1,
  alpha_dir = 1,
  prior_delta = function(d) dbeta(d, 9, 1, log = TRUE),
  prior_lambda = function(l) dgamma(l, 1, 0.1, log = TRUE)
)
}
\arguments{
\item{p_init}{Numeric vector of length K; initial true-name probabilities.}

\item{D}{Matrix or sparse matrix; K×K distance matrix with distances for neighbor pairs.}

\item{M}{Matrix or sparse matrix; K×K binary mask of neighbor edges (1 for neighbors, 0 otherwise).}

\item{n_obs}{Numeric or integer vector of length K; observed counts per name.}

\item{n_iter}{Integer; number of sampler iterations. Default \code{10000}.}

\item{delta_init}{Numeric in (0,1); initial value for \code{delta}. Default \code{0.1}.}

\item{lambda_init}{Numeric >0; initial value for \code{lambda}. Default \code{1.0}.}

\item{sd_logit}{Numeric; proposal SD for logit(\code{delta}). Default \code{0.1}.}

\item{sd_loglam}{Numeric; proposal SD for log(\code{lambda}). Default \code{0.1}.}

\item{alpha_dir}{Numeric; Dirichlet prior hyperparameter (scalar or length-K vector). Default \code{1}.}

\item{prior_delta}{Function; log-density prior for \code{delta}. Default Beta(9,1) on \code{[0,1]}.}

\item{prior_lambda}{Function; log-density prior for \code{lambda}. Default Gamma(1,0.1) on \code{(0,\infty)}.}
}
\value{
A list with components:
\describe{
  \item{delta}{Numeric vector of sampled \code{delta} values (length \code{n_iter}).}
  \item{lambda}{Numeric vector of sampled \code{lambda} values.}
  \item{likelihood}{Numeric vector of log-likelihood values at each iteration.}
  \item{p_first5}{Matrix (\code{n_iter} × \code{min(5,K)}) of the first five entries of \code{p} at each iteration.}
}
}
\description{
Performs Gibbs sampling for the garbling mixture model.
It updates the true-name probability vector \code{p} via a collapsed Gibbs step using expected counts,
and jointly updates the distortion probability \code{delta} and decay rate \code{lambda} via a Metropolis-Hastings random walk.
Only the first five entries of \code{p} are retained in the output.
}
\examples{
\dontrun{
K <- length(p_init)
out <- draw_gibbs(
  p_init     = rep(1/K, K),
  D          = D_mat,
  M          = M_mat,
  n_obs      = counts,
  n_iter     = 2000
)
head(out$p_first5)
}

}
