% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/draw_gibbs.R
\name{draw_gibbs}
\alias{draw_gibbs}
\title{Run Gibbs sampler for *name* mixture model}
\usage{
draw_gibbs(
  data,
  neighbor_list,
  lambda = 0.1,
  priors = list(),
  init = list(),
  n_iter = 20000
)
}
\arguments{
\item{data}{Data frame with columns
– **name**  : candidate name (unique identifier)
– **count** : observed integer count}

\item{neighbor_list}{Length‑K list; each element contains
$j$ : integer vector of neighbour indices (length mᵢ)
$d$ : numeric vector of edit distances (same length)}

\item{lambda}{Initial value for the error‑kernel scale parameter (default 0.1)}

\item{priors}{Optional list of prior hyper‑parameters.}

\item{init}{Optional list of initial values (excludes λ).}

\item{n_iter}{Total MCMC iterations (default 10000).}
}
\value{
A list with
  * **delta_samples**  : numeric vector of δ draws
  * **lambda_samples** : numeric vector of λ draws
  * **x_avg**         : posterior mean inclusion probability for each name
  * **p_avg**         : posterior mean of mixture weights p
}
\description{
Run Gibbs sampler for *name* mixture model
}
\examples{
df <- data.frame(name = c("a","b","c"), count = c(10,5,1))
nbrs <- list(list(j = 2,      d = 1),
             list(j = c(1,3), d = c(1,2)),
             list(j = 2,      d = 2))
out <- draw_gibbs(df, nbrs, lambda = 0.1, n_iter = 100)
}
