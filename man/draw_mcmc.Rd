% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/draw_mcmc.R
\name{draw_mcmc}
\alias{draw_mcmc}
\title{Run a joint MH sampler for the garbling mixture model}
\usage{
draw_mcmc(
  p,
  D,
  M,
  n_obs,
  n_iter = 10000,
  delta_init = 0.1,
  lambda_init = 1,
  sd_logit = 0.1,
  sd_loglam = 0.1,
  prior_delta = function(d) dbeta(d, 9, 1, log = TRUE),
  prior_lambda = function(l) dgamma(l, 1, 0.1, log = TRUE)
)
}
\arguments{
\item{p}{Numeric vector of length K; fixed true-name probabilities.}

\item{D}{K×K distance matrix (sparse or dense) with distances
for neighbor pairs and zeros elsewhere.}

\item{M}{K×K binary mask matrix (sparse or dense) with 1’s
for neighbor edges; must be symmetric.}

\item{n_obs}{Numeric or integer vector (length K) of observed counts.}

\item{n_iter}{Integer; number of sampler iterations (default: 10000).}

\item{delta_init}{Numeric in (0,1); initial value for delta (default: 0.1).}

\item{lambda_init}{Numeric > 0; initial value for lambda (default: 1.0).}

\item{sd_logit}{Numeric; proposal SD for logit(delta) random walk (default: 0.1).}

\item{sd_loglam}{Numeric; proposal SD for log(lambda) (default: 0.1).}

\item{prior_delta}{Function; log-density prior for delta (default: Beta(9,1)).}

\item{prior_lambda}{Function; log-density prior for lambda (default: Gamma(1,0.1)).}
}
\value{
A list with components:
\describe{
  \item{delta}{Numeric vector of sampled delta values (length \code{n_iter}).}
  \item{lambda}{Numeric vector of sampled lambda values.}
  \item{likelihood}{Numeric vector of log-likelihood values at each iteration.}
}
}
\description{
Performs a single Metropolis-Hastings sampling step jointly updating
the garbling probability (delta) and the exponential-decay rate (lambda)
under a fixed true-name probability vector \code{p}, using sparse or dense
matrix arithmetic via \code{loglikelihood()}. This one-step proposal
can improve mixing when \code{delta} and \code{lambda} are correlated.
}
\examples{
\dontrun{
draws <- draw_mcmc(
  p = p_vec,
  D = D_mat,
  M = M_mat,
  n_obs = counts,
  n_iter = 5000
)
}

}
