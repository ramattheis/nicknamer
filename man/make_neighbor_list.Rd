% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/make_neighbor_list.R
\name{make_neighbor_list}
\alias{make_neighbor_list}
\title{Build neighbor list for \code{draw_gibbs()} via full loop}
\usage{
make_neighbor_list(
  strings,
  method = "lv",
  max_dist = ifelse(method \%in\% c("jw", "cosine", "jaccard"), 0.3, 3),
  show_progress = TRUE
)
}
\arguments{
\item{strings}{Character vector of unique strings to compare.}

\item{method}{Character; distance method for \code{stringdist::stringdist()}, e.g. "lv", "jw".}

\item{max_dist}{Numeric; maximum distance to consider neighbors (default 3 for edit distance, or 0.3 for similarity methods).}

\item{show_progress}{Logical; if TRUE, display a progress bar (default TRUE).}
}
\value{
A list of length \code{length(strings)} where each element is a list with:
\itemize{
\item \code{j}: integer vector of neighbor indices (in the original \code{strings}).
\item \code{d}: numeric vector of corresponding distances.
}
}
\description{
\code{make_neighbor_list()} computes, for each string, distances to all others
using \code{stringdist::stringdist()}, thresholds by \code{max_dist}, and returns
lists of neighbors suitable for \code{draw_gibbs()}.
}
\examples{
s <- c("apple","apply","appla","banana","banane")
make_neighbor_list(s, method = "lv", max_dist = 3)

}
