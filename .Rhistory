# MANUALLY DEFINING DELTA AND LAMBDA FOR NOW
us_dictionary = make_bayes_choice_dictionary(
surnames$name,
nb$D,
post$p_mean,
delta = 0.1,
lambda = 100
)
#names = all_surnames
#dictionary = us_dictionary
#lambda = 100
#delta = 0.1
#method = "jw"
#ncores = 10
us_names = standardize_names(
names = all_surnames,
dictionary = us_dictionary,
lambda = 100,
delta = 0.1,
method = "jw",
ncores = 10
)
length(unique(us_dictionary$standard))
subset(us_dictionary, standard == "smith")
# Attempting to build the base US-names dictionary:
# MANUALLY DEFINING DELTA AND LAMBDA FOR NOW
us_dictionary = make_bayes_choice_dictionary(
surnames$name,
nb$D,
post$p_mean,
delta = 0.1,
lambda = 1000
)
subset(us_dictionary, standard == "smith")
devtools::document()
devtools::install()
library(data.table)
library(nicknamer)
# Reloading the tabulation of all (cleaned) surnames in the census
surnames = fread("~/Downloads/us_surnames.csv")
# Removing surnames with fewer than 10 obs and no missing chars (~4.4% of individuals omitted)
all_surnames = surnames$namelast
surnames = surnames[n>10  & !grepl("\\?",namelast)]
# Renaming columns to match draw_gibbs expectations
colnames(surnames) = c("name","count")
nb = readRDS("~/Downloads/us_names_nb.rds")
post = readRDS("~/Downloads/post_names_all.rds")
# Attempting to build the base US-names dictionary:
# MANUALLY DEFINING DELTA AND LAMBDA FOR NOW
us_dictionary = make_bayes_choice_dictionary(
surnames$name,
nb$D,
post$p_mean,
delta = 0.1,
lambda = 1000
)
subset(us_dictionary, standard == "smith")
subset(us_dictionary, standard == "williams")
length(unique(us_dictionary$standard))
# Attempting to build the base US-names dictionary:
# MANUALLY DEFINING DELTA AND LAMBDA FOR NOW
us_dictionary = make_bayes_choice_dictionary(
surnames$name,
nb$D,
post$p_mean,
delta = 0.1,
lambda = 100
)
length(unique(us_dictionary$standard))
subset(us_dictionary, standard == "smith")
subset(us_dictionary, standard == "williams")
# Attempting to build the base US-names dictionary:
# MANUALLY DEFINING DELTA AND LAMBDA FOR NOW
us_dictionary = make_bayes_choice_dictionary(
surnames$name,
nb$D,
post$p_mean,
delta = 0.1,
lambda = 1000
)
subset(us_dictionary, standard == "williams")
subset(us_dictionary, standard == "rosencrantz")
subset(us_dictionary, standard == "rosenkrantz")
fwrite(us_dictionary, "~/Downloads/us_dictionary.csv")
us_names = standardize_names(
names = tail(all_surnames,n=400),
dictionary = us_dictionary,
lambda = 1000,
delta = 0.1,
method = "jw",
ncores = 2
)
devtools::document()
devtools::install()
library(data.table)
library(nicknamer)
# Reloading the tabulation of all (cleaned) surnames in the census
surnames = fread("~/Downloads/us_surnames.csv")
# Removing surnames with fewer than 10 obs and no missing chars (~4.4% of individuals omitted)
all_surnames = surnames$namelast
surnames = surnames[n>10  & !grepl("\\?",namelast)]
# Renaming columns to match draw_gibbs expectations
colnames(surnames) = c("name","count")
nb = readRDS("~/Downloads/us_names_nb.rds")
post = readRDS("~/Downloads/post_names_all.rds")
# Attempting to build the base US-names dictionary:
# MANUALLY DEFINING DELTA AND LAMBDA FOR NOW
us_dictionary = make_bayes_choice_dictionary(
surnames$name,
nb$D,
post$p_mean,
delta = 0.1,
lambda = 1000
)
#names = all_surnames
#dictionary = us_dictionary
#lambda = 100
#delta = 0.1
#method = "jw"
#ncores = 10
us_names = standardize_names(
names = tail(all_surnames,n=400),
dictionary = us_dictionary,
lambda = 1000,
delta = 0.1,
method = "jw",
ncores = 2
)
library(data.table)
library(nicknamer)
# Reloading the tabulation of all (cleaned) surnames in the census
surnames = fread("~/Downloads/us_surnames.csv")
# Removing surnames with fewer than 10 obs and no missing chars (~4.4% of individuals omitted)
all_surnames = surnames$namelast
surnames = surnames[n>10  & !grepl("\\?",namelast)]
# Renaming columns to match draw_gibbs expectations
colnames(surnames) = c("name","count")
nb = readRDS("~/Downloads/us_names_nb.rds")
post = readRDS("~/Downloads/post_names_all.rds")
# Attempting to build the base US-names dictionary:
# MANUALLY DEFINING DELTA AND LAMBDA FOR NOW
us_dictionary = make_bayes_choice_dictionary(
surnames$name,
nb$D,
post$p_mean,
delta = 0.1,
lambda = 1000
)
names = tail(all_surnames,n=400)
dictionary = us_dictionary
lambda = 1000
delta = 0.1
method = "jw"
ncores = 2
# Throw a warning if names and dictionary$observed don't overlap
overlap = max(c(mean(names %in% dictionary$observed),
mean(dictionary$observed %in% names)))
if(overlap < 0.5){
warning("Overlap between `names` and `dictionary$observed` is small...\n Are you sure you have the right dictionary?")
}
# Creating a set of standard names from the dictionary
standard_names <- subset(dictionary, !duplicated(standard))
standard_names$observed <- NULL
# Splitting off names missing from the dictionary
missing_observed <- names[ !(names %in% dictionary$observed)] |> unique()
# Starting up workers to handle missing_names
cl <- parallel::makeCluster(ncores)
# Sending shared objects to workers
parallel::clusterExport(
cl,
varlist = c(
"stringdist",
"standard_names",
"lambda",
"delta",
"method"
),
envir = environment()
)
require(stringdist)
# Making a new dictionary for missing names
chunks <- split(missing_observed,ceiling(seq_along(missing_observed) / 100))
results_list <- pbapply::pblapply(
X   = chunks,
FUN = standardize_missing_name,
cl  = cl
)
# pull globals into locals for speed
std     <- standard_names$standard
p_std   <- standard_names$p_standard
dp      <- delta * p_std
penalty <- 1 - exp(-0.8 * (nchar(std) - 1))
# 1) distance & threshold
D <- stringdist::stringdistmatrix(names_vec, std, method = method)
names_vex = missing_observed
# 1) distance & threshold
D <- stringdist::stringdistmatrix(names_vec, std, method = method)
names_vec = missing_observed
# 1) distance & threshold
D <- stringdist::stringdistmatrix(names_vec, std, method = method)
devtools::document()
devtools::install()
library(data.table)
library(nicknamer)
# Reloading the tabulation of all (cleaned) surnames in the census
surnames = fread("~/Downloads/us_surnames.csv")
# Removing surnames with fewer than 10 obs and no missing chars (~4.4% of individuals omitted)
all_surnames = surnames$namelast
surnames = surnames[n>10  & !grepl("\\?",namelast)]
# Renaming columns to match draw_gibbs expectations
colnames(surnames) = c("name","count")
nb = readRDS("~/Downloads/us_names_nb.rds")
post = readRDS("~/Downloads/post_names_all.rds")
# Attempting to build the base US-names dictionary:
# MANUALLY DEFINING DELTA AND LAMBDA FOR NOW
us_dictionary = make_bayes_choice_dictionary(
surnames$name,
nb$D,
post$p_mean,
delta = 0.1,
lambda = 1000
)
#names = tail(all_surnames,n=400)
#dictionary = us_dictionary
#lambda = 1000
#delta = 0.1
#method = "jw"
#ncores = 2
us_names = standardize_names(
names = tail(all_surnames,n=200),
dictionary = us_dictionary,
lambda = 1000,
delta = 0.1,
method = "jw",
ncores = 2
)
library(data.table)
library(nicknamer)
# Reloading the tabulation of all (cleaned) surnames in the census
surnames = fread("~/Downloads/us_surnames.csv")
# Removing surnames with fewer than 10 obs and no missing chars (~4.4% of individuals omitted)
all_surnames = surnames$namelast
surnames = surnames[n>10  & !grepl("\\?",namelast)]
# Renaming columns to match draw_gibbs expectations
colnames(surnames) = c("name","count")
nb = readRDS("~/Downloads/us_names_nb.rds")
post = readRDS("~/Downloads/post_names_all.rds")
# Attempting to build the base US-names dictionary:
# MANUALLY DEFINING DELTA AND LAMBDA FOR NOW
us_dictionary = make_bayes_choice_dictionary(
surnames$name,
nb$D,
post$p_mean,
delta = 0.1,
lambda = 250
)
us_names = standardize_names(
names = tail(all_surnames,n=200),
dictionary = us_dictionary,
lambda = 1000,
delta = 0.1,
method = "jw",
ncores = 10
)
us_names
names_vec = tail(all_surnames,n=200)
names = all_surnames
names = all_surnames
dictionary = us_dictionary
head(us_dictionary)
# Throw a warning if names and dictionary$observed don't overlap
overlap = max(c(mean(names %in% dictionary$observed),
mean(dictionary$observed %in% names)))
if(overlap < 0.5){
warning("Overlap between `names` and `dictionary$observed` is small...\n Are you sure you have the right dictionary?")
}
# Creating a set of standard names from the dictionary
standard_names <- subset(dictionary, !duplicated(standard))
standard_names$observed <- NULL
# Splitting off names missing from the dictionary
missing_observed <- names[ !(names %in% dictionary$observed)] |> unique()
ncores = 10
# Starting up workers to handle missing_names
cl <- parallel::makeCluster(ncores)
# Sending shared objects to workers
parallel::clusterExport(
cl,
varlist = c(
"stringdist",
"standard_names",
"lambda",
"delta",
"method"
),
envir = environment()
)
require(stringdist)
# Sending shared objects to workers
parallel::clusterExport(
cl,
varlist = c(
"stringdist",
"standard_names",
"lambda",
"delta",
"method"
),
envir = environment()
)
# Making a new dictionary for missing names
missing_standard <- pbapply::pbsapply(
X   = missing_observed,
FUN = standardize_missing_name,
cl  = cl
)
delta = 0.1
lambda = 250
dictionary = us_dictionary
names = all_surnames
names_vec = tail(all_surnames,n=200)
# Throw a warning if names and dictionary$observed don't overlap
overlap = max(c(mean(names %in% dictionary$observed),
mean(dictionary$observed %in% names)))
if(overlap < 0.5){
warning("Overlap between `names` and `dictionary$observed` is small...\n Are you sure you have the right dictionary?")
}
# Creating a set of standard names from the dictionary
standard_names <- subset(dictionary, !duplicated(standard))
standard_names$observed <- NULL
# Splitting off names missing from the dictionary
missing_observed <- names[ !(names %in% dictionary$observed)] |> unique()
# clean up
parallel::stopCluster(cl)
rm(cl)
# Starting up workers to handle missing_names
cl <- parallel::makeCluster(ncores)
# Sending shared objects to workers
parallel::clusterExport(
cl,
varlist = c(
"stringdist",
"standard_names",
"lambda",
"delta",
"method"
),
envir = environment()
)
method = "jw"
# clean up
parallel::stopCluster(cl)
rm(cl)
# Starting up workers to handle missing_names
cl <- parallel::makeCluster(ncores)
# Sending shared objects to workers
parallel::clusterExport(
cl,
varlist = c(
"stringdist",
"standard_names",
"lambda",
"delta",
"method"
),
envir = environment()
)
# Making a new dictionary for missing names
missing_standard <- pbapply::pbsapply(
X   = missing_observed,
FUN = standardize_missing_name,
cl  = cl
)
names = tail(all_surnames,n=200)
names = all_surnames[2000000:2000100]
library(data.table)
library(nicknamer)
# Reloading the tabulation of all (cleaned) surnames in the census
surnames = fread("~/Downloads/us_surnames.csv")
# Removing surnames with fewer than 10 obs and no missing chars (~4.4% of individuals omitted)
all_surnames = surnames$namelast
surnames = surnames[n>10  & !grepl("\\?",namelast)]
# Renaming columns to match draw_gibbs expectations
colnames(surnames) = c("name","count")
nb = readRDS("~/Downloads/us_names_nb.rds")
post = readRDS("~/Downloads/post_names_all.rds")
# Attempting to build the base US-names dictionary:
# MANUALLY DEFINING DELTA AND LAMBDA FOR NOW
us_dictionary = make_bayes_choice_dictionary(
surnames$name,
nb$D,
post$p_mean,
delta = 0.1,
lambda = 250
)
ncores = 10
delta = 0.1
lambda = 250
method = "jw"
dictionary = us_dictionary
names = all_surnames[c(2e6+1:100),]
names = all_surnames[c(2e6+1:100)]
names
# Throw a warning if names and dictionary$observed don't overlap
overlap = max(c(mean(names %in% dictionary$observed),
mean(dictionary$observed %in% names)))
if(overlap < 0.5){
warning("Overlap between `names` and `dictionary$observed` is small...\n Are you sure you have the right dictionary?")
}
# Creating a set of standard names from the dictionary
standard_names <- subset(dictionary, !duplicated(standard))
standard_names$observed <- NULL
# Splitting off names missing from the dictionary
missing_observed <- names[ !(names %in% dictionary$observed)] |> unique()
standardize_missing_name(missing_observed[1])
standardize_missing_name(missing_observed[2])
standardize_missing_name(missing_observed[2])
standardize_missing_name(missing_observed[3])
missing_observed[3]
# Starting up workers to handle missing_names
cl <- parallel::makeCluster(ncores)
# Sending shared objects to workers
parallel::clusterExport(
cl,
varlist = c(
"stringdist",
"standard_names",
"lambda",
"delta",
"method"
),
envir = environment()
)
require(stringdist)
# Sending shared objects to workers
parallel::clusterExport(
cl,
varlist = c(
"stringdist",
"standard_names",
"lambda",
"delta",
"method"
),
envir = environment()
)
# Making a new dictionary for missing names
missing_standard <- pbapply::pbsapply(
X   = missing_observed,
FUN = standardize_missing_name,
cl  = cl
)
# clean up
parallel::stopCluster(cl)
rm(cl)
# Binding old and new dictionaries
dictionary$p_standard <- NULL
new_dictionary <- as.data.frame(cbind(missing_observed, missing_standard))
colnames(new_dictionary) <- c("observed","standard")
full_dictionary <- rbind(dictionary, new_dictionary)
full_dictionary
new_dictionary <- as.data.frame(cbind(missing_observed, missing_standard))
new_dictionary
row.names(new_dictionary) <- NULL
new_dictionary
colnames(new_dictionary) <- c("observed","standard")
full_dictionary <- rbind(dictionary, new_dictionary)
# look up the row in dictionary for each name
idx <- match(names, full_dictionary$observed)
# pull out the standard names
dictionary$standard[idx]
idx
devtools::document()
devtools::install()
library(data.table)
library(nicknamer)
# Reloading the tabulation of all (cleaned) surnames in the census
surnames = fread("~/Downloads/us_surnames.csv")
# Removing surnames with fewer than 10 obs and no missing chars (~4.4% of individuals omitted)
all_surnames = surnames$namelast
surnames = surnames[n>10  & !grepl("\\?",namelast)]
# Renaming columns to match draw_gibbs expectations
colnames(surnames) = c("name","count")
nb = readRDS("~/Downloads/us_names_nb.rds")
post = readRDS("~/Downloads/post_names_all.rds")
# Attempting to build the base US-names dictionary:
# MANUALLY DEFINING DELTA AND LAMBDA FOR NOW
us_dictionary = make_bayes_choice_dictionary(
surnames$name,
nb$D,
post$p_mean,
delta = 0.1,
lambda = 250
)
us_names = standardize_names(
names = tail(all_surnames,n=2000),
dictionary = us_dictionary,
lambda = 250,
delta = 0.1,
method = "jw",
ncores = 10
)
90000/2000
parallel::detectCores()
us_names = standardize_names(
names = all_surnames,
dictionary = us_dictionary,
lambda = 250,
delta = 0.1,
method = "jw",
ncores = 12
)
dictionary <- read.csv("misc/us_dictionary_final.csv")
